# Realtime backend override (dev only).
# Production builds: leave unset (defaults to https://api.tabcom.space)
EXPO_PUBLIC_REALTIME_URL=http://10.0.2.2:3001

# Discover Nearby → "Install Tabcom" link (Google Drive test build for now;
# swap to App Store / Play / TestFlight / invite page without code changes).
EXPO_PUBLIC_TABCOM_INSTALL_URL=https://drive.google.com/drive/folders/tabcom-test-build
