import { Text, View } from "react-native";

/** Placeholder — built out in a later build step. */
export default function ContactsScreen() {
  return (
    <View className="flex-1 bg-ink items-center justify-center">
      <Text className="text-white text-xl font-bold">Contacts</Text>
      <Text className="text-neutral-500 mt-2">Coming in a later build</Text>
    </View>
  );
}
